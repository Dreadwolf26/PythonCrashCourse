def city_country(city, country, population=""):
    city_country_combined = city + ", " + country + " - " + population
    print(city_country_combined.title())
    return city_country_combined.title()